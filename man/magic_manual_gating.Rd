% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gating_wrapper.R
\name{magic_manual_gating}
\alias{magic_manual_gating}
\title{magic_manual_gating}
\usage{
magic_manual_gating(
  gs_input,
  parent_node,
  new_child_node,
  samples_id,
  channel_x,
  channel_y,
  size_points = 0.5,
  return_list_out = F,
  input_list_out = NULL,
  return_gs_only = T,
  ...
)
}
\arguments{
\item{gs_input}{A \code{GatingSet} object.}

\item{parent_node}{Character. Name of the parent node where the new gate should
be added or updated.}

\item{new_child_node}{Character. Name of the child node to create or update.}

\item{samples_id}{Character vector. Sample names to manually gate. These names
must match \code{sampleNames(gs_input)}.}

\item{channel_x}{Character. Name of the channel to plot on the x-axis.}

\item{channel_y}{Character. Name of the channel to plot on the y-axis.}

\item{size_points}{Numeric. Point size used in the interactive gating plot.
Default is \code{0.5}.}

\item{return_list_out}{Logical. If \code{TRUE}, return only the output from
\code{magicGating()} without modifying the \code{GatingSet}. Default is \code{FALSE}.}

\item{input_list_out}{Optional list. Existing output from \code{magicGating()}
containing a \code{list_poly_gates} slot. If provided, interactive gating is
skipped and these polygon gates are added or updated in the \code{GatingSet}.}

\item{return_gs_only}{Logical. If \code{TRUE}, return only the modified \code{GatingSet}.
If \code{FALSE}, return a list containing the modified \code{GatingSet} and the
gating output. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to \code{magicGating()}.}
}
\value{
If \code{return_gs_only = TRUE}, a modified \code{GatingSet}. Otherwise, a list
with two elements: \code{gs}, the modified \code{GatingSet}, and \code{list_out}, the
gating output used to add or update the gate.
}
\description{
Perform interactive manual gating on selected samples and add or update the
resulting polygon gate in a GatingSet.
}
\details{
This function wraps \code{magicGating()} for one manual polygon gate and integrates
the resulting \code{polygonGate} into a \code{GatingSet}. If the target child node does
not already exist, it is created under \code{parent_node}. If the node already
exists, the gate is updated only for the samples present in the gating output.
}
\examples{
\donttest{
gs <- magic_manual_gating(
  gs_input = gs,
  parent_node = "root",
  new_child_node = "Live_cells",
  samples_id = c("sample_01.fcs"),
  channel_x = "FSC-A",
  channel_y = "LIVE DEAD Blue-A"
)
}
}
\keyword{flowMagic}
