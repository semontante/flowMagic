% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gating_wrapper.R
\name{magic_plot_gs_sample}
\alias{magic_plot_gs_sample}
\title{magic_plot_gs_sample}
\usage{
magic_plot_gs_sample(
  gs,
  sample_id,
  gate_names,
  size_points = 0.5,
  concavity_val = 50,
  ...
)
}
\arguments{
\item{gs}{A \code{GatingSet} object.}

\item{sample_id}{Character. Name of the sample to plot. Must be present in
\code{sampleNames(gs)}.}

\item{gate_names}{Character vector. One or more GatingSet node names to
extract and display.}

\item{size_points}{Numeric. Point size passed to \code{magicPlot()}. Default is
\code{0.5}.}

\item{concavity_val}{Numeric. Concavity parameter available for downstream
polygon plotting. Default is \code{50}.}

\item{...}{Additional arguments passed to \code{magicPlot()}, such as \code{type},
\code{size_axis_text}, \code{size_title_x}, or \code{size_title_y}.}
}
\value{
A \code{ggplot} object showing the selected sample and gated populations.
}
\description{
Plot one sample from a GatingSet with one or more gated populations overlaid.
}
\details{
This function extracts one or more gated populations from a single sample in a
\code{GatingSet} and visualizes them using \code{magicPlot()}. If multiple gate names are
supplied, the extracted gated data are merged so the selected populations are
displayed together on the same bivariate plot. Each population is labeled using
its node name.
}
\examples{
\donttest{
# Plot one gate
p1 <- magic_plot_gs_sample(
  gs = gs,
  sample_id = "sample_01.fcs",
  gate_names = "Live_cells",
  size_points = 0.5
)

# Plot multiple gates together
p2 <- magic_plot_gs_sample(
  gs = gs,
  sample_id = "sample_01.fcs",
  gate_names = c("Live_cells", "Dead_cells"),
  type = "ML",
  size_points = 0.5
)
}
}
\keyword{GatingSet}
\keyword{flowMagic}
\keyword{plotting}
