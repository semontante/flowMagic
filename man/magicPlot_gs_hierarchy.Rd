% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{magicPlot_gs_hierarchy}
\alias{magicPlot_gs_hierarchy}
\title{magicPlot_gs_hierarchy}
\usage{
magicPlot_gs_hierarchy(
  gs,
  sample_id,
  n_col_wrap = NULL,
  size_points = 0.5,
  size_title = NULL,
  concavity_val = 50,
  add_labels = TRUE,
  size_pol_name = NULL,
  auto_size = TRUE,
  return_plot_list = FALSE,
  path_output = NULL,
  plot_width = NULL,
  plot_height = NULL,
  dpi = 300,
  ...
)
}
\arguments{
\item{gs}{A \code{GatingSet} object.}

\item{sample_id}{Character. Name of the sample to plot. The value must be
present in \code{sampleNames(gs)}.}

\item{n_col_wrap}{Integer or \code{NULL}. Number of columns used to arrange the
wrapped hierarchy figure. If \code{NULL}, the number of columns is selected
automatically from the number of hierarchy plots using
\code{ceiling(sqrt(n_plots))}, producing a compact near-square layout. Default
is \code{NULL}.}

\item{size_points}{Numeric. Point size passed to \code{magicPlot_gs_gates()} and
ultimately to \code{magicPlot()}. Default is \code{0.5}.}

\item{size_title}{Numeric or \code{NULL}. Size of the title for each subplot. Each
subplot title indicates the parent population, for example
\code{"Parent pop: CD45+"}. If \code{NULL} and \code{auto_size = TRUE}, the value is
computed automatically from the grid size. If \code{NULL} and
\code{auto_size = FALSE}, the default value is \code{10}.}

\item{concavity_val}{Numeric. Concavity value used when polygon boundaries
are estimated from gated events. This value is passed to
\code{magicPlot_gs_gates()} and then to \code{magicPlot()}. Larger values generally
produce smoother polygon boundaries. Default is \code{50}.}

\item{add_labels}{Logical. If \code{TRUE}, gate names are printed inside the
polygon gates. This is passed to \code{magicPlot()}. Labels are only shown for
plotting modes that support polygon labels, such as \code{type = "dens"}.
Default is \code{TRUE}.}

\item{size_pol_name}{Numeric or \code{NULL}. Size of the gate labels printed inside
polygon gates. If \code{NULL} and \code{auto_size = TRUE}, the value is computed
automatically from the grid size. If \code{NULL} and \code{auto_size = FALSE}, the
default value is \code{4}.}

\item{auto_size}{Logical. If \code{TRUE}, automatically estimates text sizes for
axis tick labels, axis titles, polygon labels, and subplot titles based on
the number of rows and columns in the wrapped figure. User-supplied values
passed through \code{...}, such as \code{size_axis_text}, \code{size_title_x}, or
\code{size_title_y}, are respected and not overwritten. Default is \code{TRUE}.}

\item{return_plot_list}{Logical. If \code{FALSE}, return only the wrapped plot. If
\code{TRUE}, return a list containing the wrapped plot and intermediate objects
used to build it. Default is \code{FALSE}.}

\item{path_output}{Character or \code{NULL}. Full file path where the wrapped
hierarchy plot should be saved. If \code{NULL}, no file is exported. The file
format is inferred by \code{ggplot2::ggsave()} from the file extension, for
example \code{.tiff}, \code{.png}, or \code{.pdf}. Default is \code{NULL}.}

\item{plot_width}{Numeric or \code{NULL}. Width, in inches, assigned to each
individual subplot when exporting the full wrapped figure. The final
exported width is calculated as \code{plot_width * number_of_columns}. If
\code{NULL}, the value is selected automatically from the number of columns in
the wrapped figure. This argument affects only exported files created with
\code{path_output}; it does not change the interactive RStudio Plot pane
display. Default is \code{NULL}.}

\item{plot_height}{Numeric or \code{NULL}. Height, in inches, assigned to each
individual subplot when exporting the full wrapped figure. The final
exported height is calculated as \code{plot_height * number_of_rows}. If
\code{NULL}, the value is selected automatically from the number of rows in the
wrapped figure. This argument affects only exported files created with
\code{path_output}; it does not change the interactive RStudio Plot pane
display. Default is \code{NULL}.}

\item{dpi}{Numeric. Resolution used when exporting raster image formats such
as \code{.png} or \code{.tiff}. Default is \code{300}.}

\item{...}{Additional plotting arguments passed to \code{magicPlot_gs_gates()} and
then to \code{magicPlot()}. Common examples include \code{type}, \code{size_axis_text},
\code{size_title_x}, \code{size_title_y}, \code{show_legend}, \code{x_lim1}, \code{x_lim2},
\code{y_lim1}, \code{y_lim2}, and \code{aspect_ratio}.}
}
\value{
If \code{return_plot_list = FALSE}, a wrapped \code{patchwork} plot object. If
\code{return_plot_list = TRUE}, a list with the following elements:
\describe{
\item{plot}{The final wrapped hierarchy plot.}
\item{plot_list}{A named list of individual \code{ggplot} objects before wrapping.}
\item{plot_groups}{A named list describing which gate or gates were plotted in each subplot.}
\item{plot_titles}{A named list containing the parent population title for each subplot.}
\item{df_tree}{The hierarchy table returned by \code{get_hierarchy_all_pops()} and used to build the figure.}
\item{n_cols}{Final number of columns used in the wrapped plot.}
\item{n_rows}{Final number of rows used in the wrapped plot.}
\item{plot_width}{Width, in inches, assigned to each subplot for export.}
\item{plot_height}{Height, in inches, assigned to each subplot for export.}
}
}
\description{
Plot all gates from one GatingSet sample as a hierarchy-style figure.
}
\details{
This function generates a wrapped figure containing one bivariate plot for
each gating step in a \code{GatingSet} hierarchy. For each subplot, the title
indicates the parent population and the gate labels indicate the child
population or populations shown on that plot.

Gates that share the same bivariate dimensions are automatically grouped into
the same subplot. For example, if \code{NK_cells}, \code{B_cells}, and \code{T_cells} are
sibling gates drawn on the same two channels, they are displayed together in
one plot. Gates that do not share dimensions with sibling gates are plotted
individually.

The hierarchy order is determined from \code{get_hierarchy_all_pops()}, and the
final plots are combined using \code{patchwork::wrap_plots()}.

The function uses \code{get_hierarchy_all_pops()} to obtain a hierarchy table. The
table is walked row by row so that plots follow the gating hierarchy order.
If a child population has an empty \code{Dimensions} value, it is plotted alone.
If multiple child populations share the same \code{Dimensions} value, they are
grouped into the same subplot and shown together.

If \code{n_col_wrap = NULL}, the number of columns is selected automatically from
the number of generated hierarchy plots. The automatically selected layout is
intended to avoid very wide or very narrow wrapped figures.

If \code{plot_width} or \code{plot_height} are \code{NULL}, they are selected automatically
for export based on the final number of columns and rows. These values are
used only when \code{path_output} is provided.

The plotting itself is delegated to \code{magicPlot_gs_gates()}. This wrapper
determines which gates should appear in each subplot, then calls
\code{magicPlot_gs_gates()} repeatedly and combines the resulting plots.
}
\examples{
\donttest{
# Plot the full hierarchy for one sample using automatic layout
p <- magicPlot_gs_hierarchy(
  gs = gs,
  sample_id = "sample_01.fcs",
  type = "dens"
)

# Plot the full hierarchy with a user-defined number of columns
p <- magicPlot_gs_hierarchy(
  gs = gs,
  sample_id = "sample_01.fcs",
  n_col_wrap = 3,
  type = "dens"
)

# Plot and export the hierarchy figure as a TIFF file.
# plot_width and plot_height are selected automatically if left as NULL.
p <- magicPlot_gs_hierarchy(
  gs = gs,
  sample_id = "sample_01.fcs",
  type = "dens",
  path_output = "~/main/Results/sample_01_hierarchy.tiff",
  dpi = 300
)

# Plot and export using manual export dimensions
p <- magicPlot_gs_hierarchy(
  gs = gs,
  sample_id = "sample_01.fcs",
  n_col_wrap = 3,
  type = "dens",
  path_output = "~/main/Results/sample_01_hierarchy.tiff",
  plot_width = 5,
  plot_height = 4,
  dpi = 300
)

# Manually control text sizes instead of using automatic sizing
p <- magicPlot_gs_hierarchy(
  gs = gs,
  sample_id = "sample_01.fcs",
  n_col_wrap = 3,
  auto_size = FALSE,
  size_title = 10,
  size_pol_name = 4,
  size_axis_text = 8,
  size_title_x = 10,
  size_title_y = 10,
  type = "dens"
)
}
}
\keyword{GatingSet}
\keyword{flowMagic}
\keyword{hierarchy}
\keyword{plotting}
