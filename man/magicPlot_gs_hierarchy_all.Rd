% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exporting.R
\name{magicPlot_gs_hierarchy_all}
\alias{magicPlot_gs_hierarchy_all}
\title{magicPlot_gs_hierarchy_all}
\usage{
magicPlot_gs_hierarchy_all(
  gs,
  samples_id = NULL,
  path_output,
  extension = ".tiff",
  return_plot_list = FALSE,
  ...
)
}
\arguments{
\item{gs}{A \code{GatingSet} object.}

\item{samples_id}{Character vector or \code{NULL}. Names of the samples to plot.
If \code{NULL}, all samples in \code{sampleNames(gs)} are plotted. If a character
vector is provided, all values must be present in \code{sampleNames(gs)}.
Default is \code{NULL}.}

\item{path_output}{Character. Directory where the exported hierarchy plots
should be saved. This argument is different from \code{path_output} in
\code{magicPlot_gs_hierarchy()}: here it is a directory, while in
\code{magicPlot_gs_hierarchy()} it is the full output file path.}

\item{extension}{Character. File extension used for exported plots. The value
can be supplied with or without a leading dot, for example \code{".tiff"} or
\code{"tiff"}. The extension is passed indirectly to \code{ggplot2::ggsave()} through
the generated output file path. Default is \code{".tiff"}.}

\item{return_plot_list}{Logical. If \code{FALSE}, return only a named character
vector containing the exported file paths. If \code{TRUE}, return a list
containing both the exported file paths and the plot objects returned by
\code{magicPlot_gs_hierarchy()}. Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{magicPlot_gs_hierarchy()}. Common
examples include \code{n_col_wrap}, \code{size_points}, \code{auto_size}, \code{add_labels},
\code{size_pol_name}, \code{plot_width}, \code{plot_height}, \code{dpi}, \code{type},
\code{size_axis_text}, \code{size_title_x}, and \code{size_title_y}.}
}
\value{
If \code{return_plot_list = FALSE}, a named character vector containing
the exported file paths, with names corresponding to sample names. If
\code{return_plot_list = TRUE}, a list with the following elements:
\describe{
\item{paths}{A named character vector containing the exported file paths.}
\item{plots}{A named list of plot objects returned by \code{magicPlot_gs_hierarchy()}.}
}
}
\description{
Plot and export GatingSet hierarchy figures for multiple samples.
}
\details{
This function is a wrapper around \code{magicPlot_gs_hierarchy()}. It applies
\code{magicPlot_gs_hierarchy()} to multiple samples in a \code{GatingSet} and exports
one hierarchy-style plot per sample.

For each sample, the output file name is generated automatically from the
sample name by removing the file extension and appending the selected output
extension.

For example:

\preformatted{
sample name: Flow 1_BIS002.fcs
output file: Flow 1_BIS002.tiff
}

The function first determines which samples should be plotted. If
\code{samples_id = NULL}, all samples in the \code{GatingSet} are used. Otherwise, the
requested sample names are checked against \code{sampleNames(gs)}.

The output directory is created automatically if it does not already exist.
Each sample is then plotted by calling \code{magicPlot_gs_hierarchy()} with a
sample-specific full output file path.

This wrapper is useful when the same hierarchy plot should be generated for
many or all samples in a \code{GatingSet}.
}
\examples{
\donttest{
# Export hierarchy plots for all samples
paths <- magicPlot_gs_hierarchy_all(
  gs = gs,
  path_output = "~/main/Results/Plots_gated_data_hierarchy"
)

# Export hierarchy plots for selected samples only
paths <- magicPlot_gs_hierarchy_all(
  gs = gs,
  samples_id = sampleNames(gs)[1:3],
  path_output = "~/main/Results/Plots_gated_data_hierarchy"
)

# Pass plotting options to magicPlot_gs_hierarchy()
paths <- magicPlot_gs_hierarchy_all(
  gs = gs,
  samples_id = sampleNames(gs)[1:3],
  path_output = "~/main/Results/Plots_gated_data_hierarchy",
  extension = ".tiff",
  type = "dens",
  add_labels = TRUE,
  dpi = 300
)

# Return both exported paths and plot objects
out <- magicPlot_gs_hierarchy_all(
  gs = gs,
  samples_id = sampleNames(gs)[1:3],
  path_output = "~/main/Results/Plots_gated_data_hierarchy",
  return_plot_list = TRUE
)
}
}
\keyword{GatingSet}
\keyword{export}
\keyword{flowMagic}
\keyword{hierarchy}
\keyword{plotting}
