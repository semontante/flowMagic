% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Utils.R
\name{get_gate_channels}
\alias{get_gate_channels}
\title{Extract gate channels for a population}
\usage{
get_gate_channels(gs, pop)
}
\arguments{
\item{gs}{A \code{GatingSet} or \code{GatingHierarchy} object.}

\item{pop}{Character. Name of the population/node.}
}
\value{
A character vector containing the channel names used by the gate.
If the population is \code{"root"}, if no gate is found, or if the channels cannot
be extracted, an empty character vector is returned.
}
\description{
This helper function gets the gate used to define a population and extracts
the flow cytometry channels used by that gate.
}
\details{
In some cases, \code{flowWorkspace::gs_pop_get_gate()} returns the gate directly.
In other cases, it returns a list with one gate per sample. This function
handles that by using the first gate in the list.
}
\examples{
\dontrun{
get_gate_channels(gs[[1]], "B_cells")
# [1] "BV480-A" "BV650-A"
}

}
