% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gating_wrapper.R
\name{magic_template_predict}
\alias{magic_template_predict}
\title{magic_template_predict}
\usage{
magic_template_predict(
  gs_input,
  parent_node,
  channel_x,
  channel_y,
  ref_train,
  ref_model_info,
  label_to_node,
  n_cores = 1,
  concavity_val = 50,
  add_to_gs = TRUE,
  return_gs_only = FALSE,
  ...
)
}
\arguments{
\item{gs_input}{A \code{GatingSet} object.}

\item{parent_node}{Character. Name of the parent node from which raw data are
exported and under which predicted gates are added.}

\item{channel_x}{Character. Name of the channel to use on the x-axis.}

\item{channel_y}{Character. Name of the channel to use on the y-axis.}

\item{ref_train}{Data frame. Training data generated by \code{get_train_data()},
typically \code{train_out$ref_train} from \code{magic_template_train()}.}

\item{ref_model_info}{List. Trained model information returned by
\code{magicTrain()}, typically \code{train_out$ref_model_info} from
\code{magic_template_train()}.}

\item{label_to_node}{Named character vector. Names are predicted labels and
values are the corresponding GatingSet node names. For example,
\code{c("1" = "Live_cells", "2" = "Dead_cells")}.}

\item{n_cores}{Integer. Number of cores used for prediction and polygon
extraction. Default is \code{1}.}

\item{concavity_val}{Numeric. Concavity parameter passed to
\code{flowmagic_pred_to_poly_gates()}. Higher values generally produce smoother
polygon gates. Default is \code{50}.}

\item{add_to_gs}{Logical. If \code{TRUE}, convert predicted labels to polygon gates
and add or update them in the \code{GatingSet}. If \code{FALSE}, only prediction
results are returned. Default is \code{TRUE}.}

\item{return_gs_only}{Logical. If \code{TRUE}, return only the modified \code{GatingSet}.
Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{export_raw_gs_plots()}.}
}
\value{
If \code{return_gs_only = TRUE}, a modified \code{GatingSet}. Otherwise, a list
with four elements: \code{gs}, the modified or original \code{GatingSet};
\code{list_test_data}, the exported test data; \code{list_dfs_pred}, the predicted
labeled data frames; and \code{list_poly_gates}, the polygon gates generated for
each requested node.
}
\description{
Predict gated populations from a trained flowMagic template model and
optionally add the predicted gates to a GatingSet.
}
\details{
This function exports bivariate expression data from a selected GatingSet node,
predicts event labels using a previously trained flowMagic model, converts
selected predicted labels into polygon gates, and optionally adds or updates
these gates in the input \code{GatingSet}.

The \code{label_to_node} argument controls how predicted labels are mapped to
GatingSet node names. For example, \code{c("1" = "Live_cells", "2" = "Dead_cells")}
converts predicted label \code{"1"} into a node named \code{"Live_cells"} and predicted
label \code{"2"} into a node named \code{"Dead_cells"}.
}
\examples{
\donttest{
pred_out <- magic_template_predict(
  gs_input = gs,
  parent_node = "root",
  channel_x = "FSC-A",
  channel_y = "LIVE DEAD Blue-A",
  ref_train = train_out$ref_train,
  ref_model_info = train_out$ref_model_info,
  label_to_node = c("1" = "Live_cells", "2" = "Dead_cells"),
  n_cores = 1,
  add_to_gs = TRUE
)

gs <- pred_out$gs
}
}
\keyword{GatingSet}
\keyword{automated}
\keyword{flowMagic}
\keyword{gating}
\keyword{prediction}
\keyword{template}
