% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Utils.R
\name{format_channel_info}
\alias{format_channel_info}
\title{Format gate channel information}
\usage{
format_channel_info(channels, df_metadata)
}
\arguments{
\item{channels}{Character vector. Channel names used by a gate.
For example, \code{c("BV480-A", "BV650-A")}.}

\item{df_metadata}{Data frame. FlowFrame parameter metadata, usually from
\code{ff@parameters@data}. Must contain columns \code{name} and \code{desc}.}
}
\value{
A single character string containing formatted channel information,
with one channel per line.
}
\description{
This helper function takes channel names used by a gate and matches them
to the metadata stored in a flowFrame parameter table.
}
\details{
The metadata table usually comes from:

\code{ff@parameters@data}

It should contain at least the columns \code{name} and \code{desc}.

For channels with a marker description, the function returns:

\code{channel = marker}

For channels without a marker description, such as scatter channels, the
function returns only the channel name.
}
\examples{
\dontrun{
ff <- flowMagic::get_flowframe_from_gs(
    gs = gs,
    node_name = "root",
    sample_id = 1
)

df_metadata <- ff@parameters@data

format_channel_info(
    channels = c("BV480-A", "BV650-A"),
    df_metadata = df_metadata
)
# "BV480-A = CD19\nBV650-A = NK1.1"
}

}
